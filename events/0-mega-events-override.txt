namespace = mega
# Ecumenopolis complete
planet_event = {
	id = mega.200
	title = mega.200.name
	desc = mega.200.desc
	picture = GFX_evt_city_planet
	show_sound = event_cityscape
	location = root
	is_triggered_only = yes
	trigger = {
		is_planet_class = pc_city
	}
	option = {
		name = mega.200.a
		hidden_effect = {
			planet_event = {
				id = starborn.13				# random planets }
			}
			hidden_effect = {
				planet_event = {
					id = starborn.15					# home planet }
				}
			}
		}
	}
}

# Ecumenopolis complete (Relic World)
planet_event = {
	id = mega.201
	title = mega.200.name
	desc = mega.201.desc
	picture = GFX_evt_city_planet
	show_sound = event_cityscape
	location = root
	is_triggered_only = yes
	trigger = {
		is_planet_class = pc_city
	}
	option = {
		name = mega.200.a
		hidden_effect = {
			planet_event = {
				id = starborn.13				# random planets }
			}
			hidden_effect = {
				planet_event = {
					id = starborn.15					# home planet }
				}
			}
		}
	}
}
